logging:
  level: debug

monitoring:
  method: OPTIONS
  minInterval: 1000
  maxInterval: 10000
  downStatus: '^(5..|4..)$'

notifications:
  slack:
    enabled: false
    endpoint: SLACK_WEBHOOK_ENDPOINT_HERE

services:
  - name: 'Localhost'
    url: 'http://127.0.0.1'